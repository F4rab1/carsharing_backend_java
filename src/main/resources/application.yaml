spring:
  application:
    name: carsharing
  datasource:
    url: jdbc:postgresql://localhost:5432/carsharing
    username: postgres
    password: postgres
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}